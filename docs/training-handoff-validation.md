# lianjie-dev 交接检查记录

日期：2026-09-18。此次整理只调整启动默认值、相关启动回归及文档；不修改模型、损失、采样器或容量边界，也没有重新启动 NPU 训练。

- CPU 回归：按 `docs/ascend.md` 的迁移层隔离命令运行，122 项通过、18 项 deselected；后者包含 CUDA/NPU 参数及名称含 npu 的启动测试，不是测试失败。
- 启动与采样专项：完整运行 `tests/test_training_sampling_policy.py`，覆盖默认八卡命令、容量配置仅默认应用于八卡、混合来源默认分桶、单来源不分桶和显式分桶覆盖。17 项通过（与上述 CPU 回归有重叠，不相加计数）。
- Shell：混训入口通过 `bash -n`。
- dry-run：本机实际模型/数据目录下，默认命令与显式八卡、50,000 步/每 1,000 步保存的命令均成功解析。两者均记录 worker 1/2、sublayer、容量配置及分桶 10。dry-run 不加载训练模型，不进行设备训练或保存。
- Git：交接前使用 `git diff --check` 检查空白问题；仅跟踪代码、配置、测试和文档，`outputs/`、`train_out/`、权重和数据继续被忽略。

CPU 测试不能替代目标节点的设备验证。此前八卡硬件测试对应 `3ad2b52`，结果见 [训练显存记录](training-memory.md)；新默认值与吞吐子集运行时的开关一致，但子集关闭条件 dropout 并绕过全量索引构建。本次没有将短程测试表述为长程收敛或全量容量保证。

检查原始输出保留在本机 `outputs/cluster-handoff-20260918/`，不随 Git 分支发布。目标节点请按 [交接步骤](cluster-training.md)运行数据检查和包含模型保存的 10 步流程验证。
